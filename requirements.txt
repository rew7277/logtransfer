Flask>=3.0.0
Werkzeug>=3.0.0
gunicorn>=22.0.0

# Rate limiting — in-memory by default, Redis-backed when REDIS_URL is set
flask-limiter>=3.5.0
limits>=3.6.0

# AWS S3 ingestion
boto3>=1.34.0

# External API ingestion
requests>=2.31.0

# Scheduled jobs
APScheduler>=3.10.4

# PostgreSQL (for future migration from SQLite — already in use via psycopg)
psycopg[binary]>=3.2.0
psycopg2-binary>=2.9.9
